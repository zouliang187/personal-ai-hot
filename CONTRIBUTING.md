# Contributing

感谢你愿意改进 AI Hot。这个项目优先接受能提升信息质量、透明度和维护效率的贡献。

## 适合贡献的方向

- 新增或调整 AI 领域 RSS 信源。
- 改进评分、去重、分类和日报分组逻辑。
- 修复页面展示、搜索、筛选或移动端适配问题。
- 补充测试、文档、部署说明和自动化更新流程。

## 本地开发

```bash
npm install
npm run update
npm run serve
```

打开 `http://127.0.0.1:4173` 检查页面。

## 提交信源建议

新增信源时请说明：

- 信源名称和 RSS 地址。
- 为什么这个来源值得加入。
- 建议的 `tier`、`kind` 和 `category`。
- 是否存在明显重复、广告或低质量内容。

## Pull Request 建议

- 保持改动聚焦，一次 PR 解决一个明确问题。
- 如果改动评分或筛选逻辑，请说明预期影响。
- 如果更新了抓取逻辑，请运行 `npm run update` 并确认 `data/news.json` 能正常生成。
- UI 改动请在桌面和移动端宽度下各检查一次。

## 维护原则

- 评分规则应尽量可解释。
- 默认信源数量应保持克制，优先质量而不是数量。
- 静态部署优先，避免引入不必要的服务端依赖。
